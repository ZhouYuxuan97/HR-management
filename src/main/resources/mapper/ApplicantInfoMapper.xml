<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hr.mapper.ApplicantInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hr.entity.ApplicantInfo">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="sex" property="sex" />
        <result column="age" property="age" />
        <result column="job" property="job" />
        <result column="major" property="major" />
        <result column="experence" property="experence" />
        <result column="study_effort" property="studyEffort" />
        <result column="school" property="school" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="create_time" property="createTime" />
        <result column="content" property="content" />
        <result column="is_store" property="isStore" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, sex, age, job, major, experence, study_effort AS studyEffort, school, phone, email, create_time AS createTime, content, is_store AS isStore
    </sql>

</mapper>
